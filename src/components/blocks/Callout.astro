---
import { tinaField } from '@tinacms/astro/tina-field';
import Section from '../ui/Section.astro';
import Icon from '../ui/Icon.astro';
import type { CalloutBlock } from '../../lib/data';

interface Props { data: CalloutBlock }

const { data } = Astro.props;
---
<Section class="py-6">
	{data.url ? (			<a href={data.url} data-tina-field={tinaField(data, 'url')} class="hover:bg-accent hover:border-primary/30 bg-muted group mx-auto flex w-fit items-center gap-4 rounded-2xl border p-1 pl-4 transition-all duration-300">
			<span data-tina-field={tinaField(data, 'text')} class="text-foreground text-sm">{data.text}</span>
			<span class="block h-4 w-0.5 border-l bg-background"></span>
			<div class="bg-background flex size-6 items-center justify-center rounded-xl">
				<Icon name="arrow-right" class="size-3" />
			</div>
		</a>
	) : (
		<div data-tina-field={tinaField(data, 'text')} class="bg-muted mx-auto flex w-fit items-center gap-4 rounded-2xl border p-1 pl-4 transition-colors duration-300">
			<span class="text-foreground text-sm">{data.text}</span>
			<span class="block h-4 w-0.5 border-l bg-background"></span>
			<div class="bg-background flex size-6 items-center justify-center rounded-xl">
				<Icon name="arrow-right" class="size-3" />
			</div>
		</div>
	)}
</Section>
