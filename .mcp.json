{
  "_comment": "mupla-front MCP server manifest. Populated by bin/mcp-bootstrap.sh after the maintainer paste. Empty mcpServers array is intentional pre-install state. Field structure follows the MCP server-manifest convention (`mcpServers: []`). The $schema field was removed - do not reintroduce without verifying the URL against the published MCP schema index first.",
  "mcpServers": []
}
