# Triage Roadmap — 2026-07-06 (Pass 11)

30-ticket forward agenda for `daily-triage` loop on `mupla-front`. Sourced from `loop-readiness-2026-07-06.md` lever inventory, `STATE.md` Pass 11 agenda, and the visible content/TinaCMS gap surface. Every ticket respects `docs/safety.md` (close / push / remove-label = human-only) and `loop-constraints.md` (paths and op denylists). The Pass assignment column tells the next agent which day to schedule each ticket.

> **Reviewer-applied corrections (Pass 11 close-out):**
> 1. Starting score fix — `Pass 10 = 85`, all cumulative cells lifted from this single source.
> 2. Cumulative column clamped to `min(prev + delta, 100)` since `scripts/loop-audit-local.sh` caps at 100.
> 3. Effort column now carries a token estimate: `S = ~25k`, `M = ~60k`, `L = ~120k` tokens per the `loop-budget.md` 200k-per-pass cap.
> 4. `gh issue create --label needs-triage` body templates shipped as the **Body template** column (so the next agent doesn't have to derive them).
> 5. New ticket **#31 (Vercel production build debugging, T1)** added — covers the user's "resolve build issues in production vercel" ask. Sits inside Bucket A.
> 6. Tickets **#28-30 re-labeled `needs-triage`** (the agent must NEVER move issues into `ready-for-human` per `docs/safety.md`; the maintainer pastes the `--add-label ready-for-human` snippet once the email/survey system is wired).

## Bucket index

- **A — Loop infrastructure / cron / observability** (9 tickets; #1-8, #31)
- **B — Accessibility + a11y baseline** (6 tickets; #9-14)
- **C — Content carry-over / token audit / data hygiene** (6 tickets; #15-20)
- **D — TinaCMS + analytics + analytics-keyed schema migrations** (5 tickets; #21-25)
- **E — DX, agent workflow, matt-pocock skill surface, fundraising / donor analytics** (5 tickets; #26-30)

## Tier legend

- **T1** = critical-path L2 → L3 wire (≥ +5 score or unblocks prod)
- **T2** = completeness (improves quality, score-neutral or ≤ +3)
- **T3** = next-quarter (not in current sprint window)

## Effort / token legend

Each ticket's `Effort` column also carries a token estimate so the next agent can check per-Pass budget vs `loop-budget.md` (200 k cap):

| Effort | Token estimate |
|--------|----------------|
| **S**  | ~25 k          |
| **M**  | ~60 k          |
| **L**  | ~120 k         |

A pass with 4 × `L` tickets is over budget (480 k). Schedule-aware: spread `L` across passes.

## Score-delta legend

Matches the cumulative lifts in `loop-readiness-2026-07-06.md` §"L1 → L2 levers" / §"L2 → L3 levers".

---

## Bucket A — Loop infrastructure / cron / observability

| #   | Pass | Title | Label | Tier | Acceptance | Effort | Score | Body template (`gh issue create --body`) |
|-----|------|-------|-------|------|------------|--------|-------|------------------------------------------|
| 1   | 11   | Wire `.github/workflows/daily-triage.yml` (night cron, 1 pass/day) | `ready-for-agent` | T1 | File exists; cron `cron: '0 6 * * *'`; calls `bash scripts/loop-audit-local.sh` + `gh issue list --state open --json`; commits STATE.md; zero prod-only ops | **L** (~120 k) | **+10** | Bucket A / Tier T1 / Critical L2 → L3 lever. Adds score +10 to `loop-audit-local.sh` (file-presence check). Cross-ref: `loop-readiness-2026-07-06.md` §"L2 → L3 levers". Drift recovery: see `STATE.md` §"drift recovery index". |
| 2   | 12   | Scaffold `scripts/loop-context.sh` (rehydrate `run.json` → STATE.md) | `ready-for-agent` | T1 | Runs against a fixture `run.json`; emits diff to STATE.md without overwriting; idempotent | **L** (~120 k) | **+5** | Bucket A / Tier T1. Adds score +5. Cross-ref: same lever section. |
| 3   | 13   | Add `sub-agent` section to `loop-constraints.md` (maker/checker split) | `ready-for-agent` | T1 | §Sub-agent created; references `spawn_agents` and reviewer-minimax-m3 | **S** (~25 k) | **+5** | Bucket A / Tier T1. Adds score +5. |
| 4   | 14   | Document per-pass worktree policy in `loop-constraints.md` | `ready-for-agent` | T2 | §Worktree policy created; documents `git worktree add` + cleanup | **S** (~25 k) | 0 | Bucket A / Tier T2. Quality drift blocker; no score gain. |
| 5   | 15   | Register `goal-engineering` pattern in `patterns/registry.yaml` (`status: not-active`) | `ready-for-agent` | T2 | New entry under `patterns:`; matches existing schema | **S** (~25 k) | **+3** | Bucket A / Tier T2. Adds score +3. |
| 6   | 15   | Add `docs/agents/kill-switch-test-2026-XX.md` drill report | `ready-for-agent` | T2 | Drill covers all 5 kill-switch triggers from `loop-budget.md`; output green/yellow/red | **M** (~60 k) | 0 | Bucket A / Tier T2. |
| 7   | 16   | Document override flow for human-only denylist ops | `ready-for-agent` | T2 | Cross-link `loop-constraints.md` §Override flow from `docs/safety.md`; example template present | **S** (~25 k) | 0 | Bucket A / Tier T2. |
| 8   | 17   | Add `loop-engineering` MCP server status to readiness audit brief | `ready-for-agent` | T3 | Brief references `mcp_server.json` next to Tina CMS counter; falls back gracefully | **S** (~25 k) | 0 | Bucket A / Tier T3. |
| **31** | **11** | **Vercel production build debugging (TinaCloud rebuild hook + `astro build` OOM @ 3072 MiB)** | **ready-for-agent** | **T1** | **`pnpm build --log-level debug` runs locally; `astro build` chunk budget raised; `vercel.json` updated; hook dry-run documented; maintains ⟶ commit ⟶ push ⟶ dep gate** | **L** (~120 k) | **0** (fix-not-feature; recoups lost score after schema mismatch billable) | **Bucket A / Tier T1. User-flags "resolve build issues in prodcuton vercel". Cross-ref: `CONTEXT-site.md` §"Memory budget at build"; net ticket to retire #3 once main builds green end-to-end. Drift recovery: included in `STATE.md` §"drift recovery index" once confirmed.** |

**Subtotal A across Pass 11-17: +5 +5 +3 +10 +0 = +23 points** (script cap clamps).\n\n## Bucket B — Accessibility + a11y baseline\n\n| #  | Pass | Title | Label | Tier | Acceptance | Effort | Score | Body template |\n|----|------|-------|-------|------|------------|--------|-------|---------------|\n| 9  | 11   | Scaffold `scripts/axe-core.sh` (axe invocation via headless Chrome + Playwright) | `ready-for-agent` | T1 | Runs against `astro preview`; exits 0 on green, 1 on violation; logs violations JSON to `docs/agents/`; idempotent | **L** (~120 k) | **+5** | Bucket B / Tier T1. Adds score +5. |\n| 10 | 12   | Generate `docs/agents/a11y-baseline-2026-XX-XX.md` (axe-core sweep on home, donate, contact) | `ready-for-agent` | T2 | Report covers ≥ 8 a11y rules per page; baseline fixed for Pass 13-16 delta detection | **M** (~60 k) | 0 | Bucket B / Tier T2. |\n| 11 | 13   | Add `aria-live` annotations on the donation tier CTAs | `ready-for-agent` | T2 | `cta.template.ts` actions render `aria-live="polite"` on focus; screen-reader announces tier selection | **S** (~25 k) | 0 | Bucket B / Tier T2. |\n| 12 | 14   | Audit focus trap on modal/dialog components | `ready-for-agent` | T2 | Tab cycles back to first focusable; Esc closes; add `inert` on closed state | **M** (~60 k) | 0 | Bucket B / Tier T2. |\n| 13 | 15   | Document keyboard navigation in DESIGN.md §8 | `ready-for-agent` | T2 | §8 has skip-link + tab-order + focus-ring rules | **S** (~25 k) | 0 | Bucket B / Tier T2. |\n| 14 | 16   | Reduce-motion Honour (carve-out, oxfam.astro) | `ready-for-agent` | T3 | When `prefers-reduced-motion: reduce`, oxfam carousel ceases auto-rotate | **S** (~25 k) | 0 | Bucket B / Tier T3. |\n\n## Bucket C — Content carry-over / token audit / data hygiene\n\n| #  | Pass | Title | Label | Tier | Acceptance | Effort | Score | Body template |\n|----|------|-------|-------|------|------------|--------|-------|---------------|\n| 15 | 11   | Phone/address cleanup on `contact.mdx` + `faq.mdx` | `ready-for-agent` | T1 | No `(555) 123-4567` or `123 Community Way, Springfield, USA` literals on visitor surface; replaced with `[phone-add-in-tina-admin]` / `[address-add-in-tina-admin]` markers | **S** (~25 k) | 0 | Bucket C / Tier T1. |\n| 16 | 12   | Add `phone` + `addressLine1` + `addressLine2` + `city` to `tina/collections/global-config.ts` | `ready-for-agent` | T1 | Additive (`required: false`); regen passes; existing config.json parses unchanged | **M** (~60 k) | 0 | Bucket C / Tier T1. |\n| 17 | 13   | Surface processSteps / Zakat-contained variant in CMS config | `needs-info` | T2 | Maintainer answer required; ticket re-opens with concrete acceptance | **S** (~25 k) | 0 | Bucket C / Tier T2. Maintainer input needed before agent can specify. |\n| 18 | 14   | Page section budget track (count sections per page; warn if < 6) | `ready-for-agent` | T2 | `scripts/page-budget.sh` audits Pages collection; exit 1 if any page < 6 sections | **S** (~25 k) | 0 | Bucket C / Tier T2. |\n| 19 | 15   | Token audit on `oxfam.astro` (carve-out test) | `ready-for-agent` | T3 | Audit brief lists oxfam-specific carve-outs | **M** (~60 k) | 0 | Bucket C / Tier T3. |\n| 20 | 17   | Voice spot-check on fresh content using `~/.agents/skills/brand/SKILL.md` | `ready-for-agent` | T3 | Last 30 blog posts scanned; deviations called out with px-ratios | **M** (~60 k) | 0 | Bucket C / Tier T3. |\n\n## Bucket D — TinaCMS + analytics + analytics-keyed schema migrations\n\n| #  | Pass | Title | Label | Tier | Acceptance | Effort | Score | Body template |\n|----|------|-------|-------|------|------------|--------|-------|---------------|\n| 21 | 11   | `data-tier` schema migration: enum on `cta.template.ts` actions | `ready-for-agent` | T1 | Enum `friend | supporter | patron | benefactor | undefined`; renders `data-tier=\"<value>\"` on `<a>`; existing chips parse; regen passes | **M** (~60 k) | 0 | Bucket D / Tier T1. Additive schema — within `loop-constraints.md` denylist. |\n| 22 | 13   | Wire analytics on `cta.template.ts` tier clicks (data attr → Plausible custom event) | `ready-for-agent` | T1 | Plausible script registered; `plausible('tier-click', {props: {tier}})` fires on click | **M** (~60 k) | 0 | Bucket D / Tier T1. |\n| 23 | 15   | `publishStatus` enum on `tina/collections/blog.ts` | `ready-for-agent` | T2 | Additive with default `published`; existing posts parse unchanged | **S** (~25 k) | 0 | Bucket D / Tier T2. |\n| 24 | 16   | `category` enum / derived on `tina/collections/blog.ts` | `ready-for-agent` | T2 | Either literal-enum or frontmatter-derived; schemas validated against current 5 posts | **M** (~60 k) | 0 | Bucket D / Tier T2. |\n| 25 | 17   | Author byline schema → reference `team` collection. **NOTE: de-normalisation pass is content mass-rewrite; recommend maintainer eyeball before agent migration** | `ready-for-agent` | T3 | New `team` collection with name + bio + role; blog.author becomes type:reference | **L** (~120 k) | 0 | Bucket D / Tier T3. **Risk: existing authors de-normalised on a one-shot pass — content-rewrite risk; maintainer should eyeball first.** |\n\n## Bucket E — DX, agent workflow, matt-pocock skill surface, fundraising / donor analytics\n\n| #  | Pass | Title | Label | Tier | Acceptance | Effort | Score | Body template |\n|----|------|-------|-------|------|------------|--------|-------|---------------|\n| 26 | 16   | Wire `~/.agents/skills/writing-shape` invocation quick-load in AGENTS.md | `needs-info` | T2 | Maintainer confirms install vs surface-in-repo only; ticket flips to `ready-for-agent` once confirmed | **S** (~25 k) | 0 | Bucket E / Tier T2. |\n| 27 | 12   | Wire `donate.mdx` data-tier analytics to Plausible first-touch | `ready-for-agent` | T1 | Plausible goal `donate-tier-click-friend` set up; SSR-rendered `<a>` carries data attribute | **M** (~60 k) | 0 | Bucket E / Tier T1. |\n| 28 | 14   | Donor thank-you email sequence (post-donate webhook) — **agent creates with `needs-triage`; maintainer pastes `ready-for-human` once email infra is wired** | `needs-triage` | T2 | Stripe webhook stub + email template scaffold; final copy is maintainer-owned | **L** (~120 k) | 0 | Bucket E / Tier T2. **Maintainer moves to `ready-for-human` once email infra is wired.** |\n| 29 | 15   | Survey post-event satisfaction form (via ContactForm block config) — **agent creates with `needs-triage`; maintainer pastes `ready-for-human`** | `needs-triage` | T3 | Plug ContactForm block into Event collection post-event variant | **L** (~120 k) | 0 | Bucket E / Tier T3. **Maintainer-owned `ready-for-human` transition.** |\n| 30 | 16   | Recurring-gift opt-out link on receipt — **agent creates with `needs-triage`; maintainer pastes `ready-for-human`** | `needs-triage` | T3 | Email template edit + mailto-pause script on tier list | **M** (~60 k) | 0 | Bucket E / Tier T3. **Maintainer-owned `ready-for-human` transition.** |\n\n---\n\n## Cumulative score impact (clamped at `min(prev + delta, 100)`)\n\nStarting point: `Pass 10 = 85/100` (verified by `bash scripts/loop-audit-local.sh`).\n\n| Pass | Tickets | Score delta | Cumul (clamped) | Tier |\n|------|---------|------------:|----------------:|------|\n| 10 (prior) | — | — | **85** | L2 → L3 |\n| 11 | 1, 9, 15, 21, 31 | +15 | **100** | L3 |\n| 12 | 2, 10, 16, 27 | +5 | **100** (cap) | L3 |\n| 13 | 3, 11, 17, 22 | +5 | **100** (cap) | L3 |\n| 14 | 4, 12, 18, 28 | 0 | **100** (cap) | L3 |\n| 15 | 5, 6, 13, 19, 23, 29 | +3 | **100** (cap) | L3 |\n| 16 | 7, 14, 24, 26, 30 | 0 | **100** (cap) | L3 |\n| 17 | 8, 20, 25 | 0 | **100** (cap) | L3 |\n\n**Cumulative adds 23 score points across Pass 11-17; script clamps to 100 at Pass 11.** Pass 12+ are score-flat but quality-flat improving. Stop counting score after `100`; keep counting drift-recovery + content coverage.\n\n## Loop-engineering primitive summary\n\n| Primitive | Current | Target | Pass where target hit |\n|-----------|--------:|-------:|------------------------|\n| Automations / Scheduling | 10/15 | 15/15 | 11 (cron) |\n| Worktrees | 0/15 | 15/15 | 14 (policy) |\n| Skills | 15/15 | 15/15 | ✓ already |\n| Plugins & Connectors (axe-core) | 15/20 | 20/20 | 11 (axe-core.sh) |\n| Sub-agents (maker/checker) | 0/15 | 15/15 | 13 |\n| Memory / State | 40/40 | 40/40 | ✓ already |\n\n## Cross-walks\n\n- `loop-readiness-2026-07-06.md` — score arithmetic basis; matches Levers 1-6.\n- `loop-design-checklist.md` — primitive cross-walk with current/target columns.\n- `docs/safety.md` — human-only op list / CRUD-triage contract.\n- `loop-constraints.md` — path / op denylists.\n- `loop-budget.md` — per-pass resource caps (200 k tokens).\n- `patterns/registry.yaml` — existing pattern list (7 catalogued, 1 active).\n- `redesign-roadmap.md` §3.1 — pass history (append-only; cross-link here from §3.1).
